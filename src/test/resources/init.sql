CREATE TABLE IF NOT EXISTS EMPLOYEE
(
    id INT AUTO_INCREMENT PRIMARY KEY,
    employeeId INT,
    firstName varchar(255),
    lastName varchar(255),
    emailAddress varchar(255),
    phoneNumber varchar(255),
    address varchar(255),
    dateOfJoiningDate varchar(255)
);
